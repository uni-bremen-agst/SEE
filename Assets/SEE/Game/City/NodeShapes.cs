namespace SEE.Game.City
{
    /// <summary>
    /// How (leaf and inner) graph nodes should be depicted.
    /// </summary>
    public enum NodeShapes : byte
    {
        Blocks,
        Cylinders,
        Spiders,
        Polygons,
        Bars
    }
}
