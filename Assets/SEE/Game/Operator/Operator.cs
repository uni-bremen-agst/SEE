/// <summary>
/// Home of operators. An operator is a component managing operations done on a
/// single game object it is attached to, such as a node or an edge.
/// They allow to manipulate the object they operate on for things like
/// movement, color changes, etc. Their underlying object should be manipulated
/// only by the operator, such that the operator can identify conflicting
/// attempts to manipulate the object.
/// </summary>
namespace SEE.Game.Operator
{
}
